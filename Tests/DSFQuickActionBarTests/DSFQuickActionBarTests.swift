import XCTest
@testable import DSFQuickActionBar

final class DSFQuickActionBarTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DSFQuickActionBar().text, "Hello, World!")
    }
}
